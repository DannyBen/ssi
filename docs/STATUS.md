# Status

## Done (Verified)
- bin command pair  (install + uninstall)
- man command pair (install + uninstall)
- completion command pair (install + uninstall)

## Next
- Create a bootstrap script, to allow users to download ssi to a temp dir and use it for installing their own stuff
- Create initial README
- Add checksum verification policy (bootstrap + URL downloads)
- Consider adding `log debug` in key places
- Consider `--user` / `--system` flags for uninstall commands to limit scope.
